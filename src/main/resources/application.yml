server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AndreySdob/config-repo
          search-paths: '{application}'
          clone-on-start: true